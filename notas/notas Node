Forma original
1. npm init => inicializar proyecto
2. npm install express
3. npm install --save path
4. npm install body-parser
5. npm install g- nodemon

Forma Rápida con SQL
1.1. express --view=ejs 
1.3 npm install
1.4 npm install mysql
1.5 npm start => arrancar el proyecto. Indicar nodemon
EN EL HTML
<form action="/" method="post"> //hay que poner en action el / y en method post 
para que extraiga los datos
--En public hay que meter img, css y todo lo que lleve el html
